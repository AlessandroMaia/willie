[package]
name = "willie-linux"
description = "Linux-side helpers shared by willied, willie-sess, willie-cli."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
willie-core = { workspace = true }
willie-harness = { workspace = true }
willie-proto = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# The tests compare the syscall filter's numbers and encodings with this
# libc's; the library itself stays libc-free on every target.
[target.'cfg(target_os = "linux")'.dev-dependencies]
libc = { workspace = true }

[lints]
workspace = true
